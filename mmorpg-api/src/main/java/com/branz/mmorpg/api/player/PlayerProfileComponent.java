package com.branz.mmorpg.api.player;

/** A separately dirty-tracked part of a persistent player profile. */
public enum PlayerProfileComponent {
    IDENTITY,
    CHARACTER_CLASS,
    LOADOUT,
    RESPAWN,
    SETTINGS
}
