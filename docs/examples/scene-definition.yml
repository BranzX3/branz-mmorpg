definition_id: scene.character.local_hub
schema_version: 1
topology: LOCAL_CHARACTER
open_requirements:
  engagement_forbidden:
    - ENGAGED
  grounded: true
  mounted: false
environment:
  provider: LOCAL_WORLD
actor:
  provider: OWNER_ONLY_MANNEQUIN
  preferred_distance: 2.75
viewpoint:
  provider: LOCAL_FRAME
  teleport_player: false
overlay:
  provider: INVENTORY
  viewport_reservation: CENTER
movement:
  normal_input: LOCK
modes:
  HUB:
    interaction: READ_ONLY
  EQUIPMENT:
    interaction: PREVIEW_COMMIT
  WARDROBE_DYE:
    interaction: PREVIEW_COMMIT
  COMBAT_ARTS:
    interaction: PREVIEW_COMMIT
  FORMS:
    interaction: PREVIEW_COMMIT
    confirm_requires_rest_context: true
  MAGIC_ATTUNEMENT:
    interaction: PREVIEW_COMMIT
    confirm_requires_rest_context: true
  CHARACTER_INFORMATION:
    interaction: READ_ONLY
close_triggers:
  - DAMAGE
  - FORCED_MOVEMENT
  - TELEPORT
  - DEATH
  - WORLD_CHANGE
  - DISCONNECT
  - ACTOR_INVALIDATED
  - SESSION_INVALIDATED
