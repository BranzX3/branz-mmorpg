package com.branz.mmorpg.worldloop.reward;

public enum RewardIneligibilityReason {
    JOINED_AFTER_CUTOFF,
    INVALID_MEMBERSHIP,
    INACTIVE,
    INSUFFICIENT_CONTRIBUTION,
    ALREADY_GRANTED
}
