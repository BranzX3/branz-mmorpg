name: playable-v1 primary coordinator patch v2

on:
  push:
    branches: [playable-v1]
    paths:
      - '.github/workflows/playable-v1-primary-coordinator-patch-v2.yml'

permissions:
  contents: write

jobs:
  patch-format-verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: playable-v1
          fetch-depth: 0
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '25'
          cache: gradle
      - name: Apply guarded adapter patch
        run: python3 .github/scripts/playable-v1-primary-coordinator.py
      - name: Apply repository formatting
        run: bash ./gradlew spotlessApply
      - name: Clean staging helpers
        shell: bash
        run: |
          rm -f .github/scripts/playable-v1-primary-coordinator.py
          rm -f .github/workflows/playable-v1-primary-coordinator-patch-v2.yml
          git diff --check
      - name: Verify full repository
        run: bash ./gradlew clean build
      - name: Commit verified patch
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'playable-v1-bot'
          git config user.email 'playable-v1-bot@users.noreply.github.com'
          git add -A
          git diff --cached --check
          git commit -m 'test(combat): centralize primary attack input routing'
          git push origin HEAD:playable-v1
