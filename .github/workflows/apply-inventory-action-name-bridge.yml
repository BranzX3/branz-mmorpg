name: apply-inventory-action-name-bridge
on:
  push:
    branches:
      - agent/physical-inventory-paper-adapter
    paths:
      - scripts/apply_inventory_action_name_bridge.py
      - .github/workflows/apply-inventory-action-name-bridge.yml
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/physical-inventory-paper-adapter
      - name: Apply action-name bridge
        run: python scripts/apply_inventory_action_name_bridge.py
      - name: Remove temporary patch tooling
        run: |
          git rm scripts/apply_inventory_action_name_bridge.py .github/workflows/apply-inventory-action-name-bridge.yml
      - name: Commit bridge
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add mmo-bootstrap/src/main/java/com/branz/mmorpg/bootstrap/PhysicalInventoryInteractionController.java
          git commit -m "Bridge Paper inventory actions into pure policy"
          git push origin HEAD:agent/physical-inventory-paper-adapter
